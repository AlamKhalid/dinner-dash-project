$('#total-price-cart').html('Rs. <%= @cart.total_price %>')
<% if @cart_item.quantity == 1 %>
  $('#dec-btn-cart-item-<%= @item_id %>').addClass("disabled")
<% else %>
  $('#dec-btn-cart-item-<%= @item_id %>').removeClass("disabled")
<% end %>
